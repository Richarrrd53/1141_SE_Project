# 快速測試指南

## 測試前準備

### 1. 執行資料庫更新
在你的 PostgreSQL 資料庫中執行 `database_schema_update.sql` 檔案：

```bash
# 使用 psql 命令列工具
psql -h ep-delicate-pond-a16i6tdf-pooler.ap-southeast-1.aws.neon.tech -U neondb_owner -d neondb -f database_schema_update.sql

# 或者手動複製 SQL 內容並在資料庫管理工具中執行
```

### 2. 創建必要的資料夾
```powershell
New-Item -ItemType Directory -Force -Path "html\uploads\proposals"
New-Item -ItemType Directory -Force -Path "html\uploads\deliveries"
```

### 3. 啟動伺服器
```bash
uvicorn main:app --reload
```

伺服器會在 http://127.0.0.1:8000 啟動

## 測試流程

### 測試 1: 截止時間（分鐘級別）

1. 以**委託人**身分登入
2. 點擊「建立新委託專案」
3. 填寫專案資訊
4. 在「截止時間（分鐘）」欄位輸入值（如：60 = 1小時，1440 = 1天，10080 = 7天）
5. 發布專案
6. 在專案列表查看截止日期是否正確計算

**預期結果**: 截止日期 = 創建時間 + 輸入的分鐘數

---

### 測試 2: 提案計畫書上傳

1. 以**接案人**身分登入
2. 瀏覽開放專案
3. 選擇一個專案並點擊進入
4. 填寫報價金額
5. **必須選擇一個 PDF 檔案上傳**（測試時準備一個 PDF 檔案）
6. 點擊「送出報價」

**預期結果**:
- ✅ 上傳成功，報價已送出
- ✅ 檔案儲存在 `html/uploads/proposals/` 資料夾
- ✅ 檔名格式：`proposal_{專案ID}_{接案人ID}_{時間戳}_{原始檔名}.pdf`

**測試驗證 - 非 PDF 檔案**:
- 嘗試上傳 .docx 或 .txt 檔案
- **預期結果**: 顯示錯誤訊息「提案計畫書必須為 PDF 格式」

---

### 測試 3: 查看提案計畫書

1. 以**委託人**身分登入
2. 進入你的專案詳情頁
3. 查看「收到的報價」表格

**預期結果**:
- ✅ 表格顯示「提案計畫書」欄位
- ✅ 有上傳 PDF 的報價顯示「下載 PDF」按鈕
- ✅ 點擊按鈕可下載並開啟 PDF 檔案

---

### 測試 4: 接受報價

1. 以**委託人**身分
2. 在報價列表中點擊「接受此報價」
3. 確認接受

**預期結果**:
- ✅ 專案狀態變更為「進行中」
- ✅ 其他報價被拒絕
- ✅ 被選中的接案人收到通知
- ✅ 未被選中的接案人收到拒絕通知

---

### 測試 5: 交付檔案（版本 1）

1. 以**接案人**身分登入
2. 進入被接受的專案
3. 點擊「上傳結案檔案」
4. 選擇任意檔案（.zip, .pdf, .jpg 等）
5. 點擊「上傳結案」

**預期結果**:
- ✅ 檔案成功上傳
- ✅ 顯示訊息「結案檔案版本 1 上傳成功！」
- ✅ 檔案儲存在 `html/uploads/deliveries/`
- ✅ 檔名格式：`delivery_{專案ID}_v1_{時間戳}_{原始檔名}`
- ✅ 專案狀態變更為「已交付」
- ✅ 委託人收到通知

---

### 測試 6: 查看版本歷史

1. 以**委託人**身分登入
2. 進入已交付的專案

**預期結果**:
- ✅ 顯示「所有版本歷史」表格
- ✅ 表格顯示：
  - 版本號：版本 1
  - 上傳時間
  - 下載按鈕
  - 狀態：待審核

---

### 測試 7: 退回案件

1. 以**委託人**身分
2. 在專案詳情頁點擊「退件」按鈕
3. 確認退件

**預期結果**:
- ✅ 顯示成功訊息
- ✅ 專案狀態變更為「已退回」
- ✅ 版本 1 的狀態標記為「已退回」（紅色）
- ✅ 接案人收到退件通知
- ✅ 接案人可以重新上傳檔案

---

### 測試 8: 重新上傳（版本 2）

1. 以**接案人**身分登入
2. 進入被退回的專案
3. 看到訊息「委託人已退件，請重新上傳檔案」
4. 選擇新的檔案
5. 點擊「上傳結案」

**預期結果**:
- ✅ 新檔案上傳成功
- ✅ 顯示訊息「結案檔案版本 2 上傳成功！」
- ✅ 新檔案檔名：`delivery_{專案ID}_v2_{時間戳}_{原始檔名}`
- ✅ **原版本 1 的檔案仍然存在**（未被覆蓋）
- ✅ 專案狀態再次變更為「已交付」
- ✅ 委託人收到新版本通知

---

### 測試 9: 多版本歷史

1. 以**委託人**身分查看專案
2. 查看版本歷史表格

**預期結果**:
- ✅ 顯示兩個版本：
  - 版本 2（最新）- 狀態：待審核
  - 版本 1 - 狀態：已退回
- ✅ 兩個版本都有下載按鈕
- ✅ 兩個下載按鈕都可正常下載對應的檔案

---

### 測試 10: 接受結案

1. 以**委託人**身分
2. 點擊「接受結案」按鈕
3. 確認接受

**預期結果**:
- ✅ 顯示成功訊息
- ✅ 專案狀態變更為「已完成」
- ✅ 最新版本（版本 2）狀態標記為「已接受」（綠色）
- ✅ 接案人收到結案通知
- ✅ 委託人收到結案確認通知
- ✅ 所有版本歷史仍然可查看和下載

---

### 測試 11: 檔名防覆蓋驗證

**提案計畫書測試**:
1. 讓兩個不同接案人對同一專案投標
2. 兩人都上傳檔名完全相同的 PDF（如 `proposal.pdf`）

**預期結果**:
- ✅ 兩個檔案都成功上傳
- ✅ 實際儲存的檔名不同：
  - `proposal_35_12_20231215_143022_proposal.pdf`
  - `proposal_35_13_20231215_143030_proposal.pdf`
- ✅ 兩個檔案都可以正常下載，內容不同

**交付檔案測試**:
1. 接案人上傳多個版本，每次都使用相同檔名（如 `final.zip`）

**預期結果**:
- ✅ 每個版本都成功上傳
- ✅ 檔名包含版本號，不會覆蓋：
  - `delivery_35_v1_20231215_150000_final.zip`
  - `delivery_35_v2_20231216_100000_final.zip`
- ✅ 所有版本都可獨立下載

---

## 資料庫驗證

### 檢查 bids 表
```sql
SELECT id, project_id, freelancer_id, bid_amount, proposal_file_path 
FROM bids 
WHERE project_id = YOUR_PROJECT_ID;
```

**預期結果**: `proposal_file_path` 欄位有儲存 PDF 檔案路徑

### 檢查 delivery_versions 表
```sql
SELECT * FROM delivery_versions 
WHERE project_id = YOUR_PROJECT_ID 
ORDER BY version DESC;
```

**預期結果**:
- 每次上傳都有新記錄
- version 依序遞增（1, 2, 3...）
- status 正確反映狀態（pending/accepted/rejected）
- file_path 包含完整的檔案路徑

---

## 常見問題排查

### 問題 1: 上傳 PDF 時顯示「提案計畫書必須為 PDF 格式」
**原因**: 檔案副檔名不是 `.pdf`
**解決**: 確保檔案是真正的 PDF 格式，副檔名為 `.pdf`

### 問題 2: 找不到 uploads 資料夾
**原因**: 未創建必要的資料夾
**解決**: 執行資料夾創建命令（見「測試前準備」）

### 問題 3: 資料庫錯誤
**原因**: 未執行 SQL 更新腳本
**解決**: 執行 `database_schema_update.sql`

### 問題 4: 版本號沒有遞增
**原因**: delivery_versions 表未正確創建
**解決**: 檢查資料庫是否有 delivery_versions 表

### 問題 5: 下載按鈕無法點擊或 404
**原因**: 檔案路徑不正確
**解決**: 
1. 檢查 `html/uploads/` 資料夾中是否有檔案
2. 檢查 main.py 是否有掛載靜態檔案路由
3. 確認檔案路徑格式正確（相對路徑從 uploads/ 開始）

---

## 成功標準

所有測試通過後，應該能夠：
- ✅ 委託人可設定精確的截止時間（分鐘）
- ✅ 接案人報價時必須上傳 PDF 提案計畫書
- ✅ 不同接案人的相同檔名不會互相覆蓋
- ✅ 委託人可查看並下載所有投標者的提案計畫書
- ✅ 委託人可選擇接案人
- ✅ 接案人可上傳交付檔案
- ✅ 委託人可退回檔案
- ✅ 接案人可重新上傳新版本
- ✅ 所有版本完整保留不覆蓋
- ✅ 委託人和接案人都能查看完整版本歷史
- ✅ 每個版本都可以獨立下載
- ✅ 委託人可接受最終版本並結案
